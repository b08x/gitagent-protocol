interface:
  display_name: "Gitagent Protocol"
  short_description: "Repo-specific patterns and workflows for gitagent-protocol"
  default_prompt: "Use the gitagent-protocol repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true